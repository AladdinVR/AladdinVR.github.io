export type FeaturedPostProps = {
  post: {
    date: string;
    description: string;
    image: string;
    imageLabel: string;
    title: string;
  };
};

export type MarkdownProps = {
  fileName: string;
};
